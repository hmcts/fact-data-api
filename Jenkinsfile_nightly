#!groovy

properties([
  pipelineTriggers([cron('H 08 * * 1-5')])
])

@Library("Infrastructure@2.7.0")

def type = "java"
def product = "fact"
def component = "data-api"

withNightlyPipeline(type, product, component) {}
